# 2026-08-16 (ninth pass) — CW Email Request, Printer tab: Serves gate + Request Type exclusivity

Converted Printer to match the Serves-gate/exclusivity convention Keycard and Phone already use (both added earlier the same day): nothing in the tab shows until "Cubework" or "Tenant" is ticked, and ticking one now unticks the other (previously independent). Everything below the Serves checkboxes moved into a new `#era_pr_body` (starts `era-field-hidden`); `eraApplyPrinterVisibility()` now also toggles it based on `cubework || tenant`. New `change` listeners force mutual exclusivity between `era_pr_cubework`/`era_pr_tenant`. Separately, "Setup New Printer", "Troubleshoot", and "New/Replace Printer Request" became mutually exclusive too, via new `ERA_PR_CHECK_IDS`/`eraApplyPrinterCheckExclusivity()` (mirrors Phone's `ERA_PH_CHECK_IDS`). No changes needed to `eraApplyPrinterVisibility()`'s existing Cubework/Tenant-combo precedence logic — it already handled multiple type checkboxes defensively, and now at most one is ever true. `functions/emailRequest.js` untouched — no server-side validation change requested, existing combo branches already only ever see one type checkbox true.

Files: `public/index.html`
