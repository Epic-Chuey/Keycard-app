# 2026-08-15 (sixth pass): Standup Report — Cali Issues/Outside Cali Issues now pull from the Issue tab

Standup's "2. Report" step's "Cali Issues:"/"Outside Cali Issues:" text now sources from the Issue tab's own Active issues per region (Title + full Notes, via new `fetchActiveIssueItemsFresh()`/`activeIssueItemsForRegion()`/`issueItemAsIsText()`/`buildIssueCaliIssuesIntroText()` in `src/app.js`) instead of today's Standup pull; Section 2 and Kuan's colored per-tag breakdown (`buildStandupSummaryDetailSectionsHtml`) stay checklist-only, unchanged. The "Open in Outlook Web" button (renamed "Send Cali/Outside Cali Email") no longer builds a deeplink — it sends the same content for real via a new `sendStandupCaliIssuesEmail` callable (`functions/index.js`, Microsoft Graph via `getGraphClientForSend()`, color-coded per tag via `buildIssueCaliIssuesEmailHtml()`), a deliberate tradeoff (immediate send, no compose-window preview first) since a deeplink body can't carry real color. The Saved step's own past-day Summary editor, the Excel export's Cali/Outside Cali sheets, and the location-catalog prompts are all untouched and now show different content than this Report step.

Files: `src/app.js`, `public/app.js`, `functions/index.js`, `public/index.html`, `docs/pitfalls.md`
