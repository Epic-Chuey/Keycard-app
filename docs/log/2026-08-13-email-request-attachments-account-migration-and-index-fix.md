# 2026-08-13: Email Request (Attachments) Apps Script project moved to the correct account, standalone index.html brought up to date

The `Google Script/email-request-attachments/` Apps Script project was found signed in as Huy's personal Gmail (`huynguyen909@gmail.com`) instead of `huy.nguyen@cubework.com`; recreated fresh under the correct account with the same `Code.gs`/`appsscript.json`. While there, the standalone `index.html` (previously only fixed for Keycard back on 2026-08-10) was brought up to date with `Code.js`: added a 6th "Laptop" tab, and gave Wi-Fi/Printer/Phone/Application-Software/Laptop the same repeatable "+ Add another entry" mechanic Keycard already had, with separate Company Name/Tenant Name fields matching `buildWifi()`/`buildGeneric()`'s `entries[]` shape. Redeployed (Execute as Me, access Anyone) at a new `/exec` URL. `Google Script/email-request-attachments/.clasp.json` still points at the old scriptId (personal-Gmail project) — repoint before trusting `clasp push`/`pull` there again; `SETUP.md` and `docs/email-request-attachments-embed-tab.md` also still reference the old scriptId/account.

Files: `Google Script/email-request-attachments/index.html`, `Google Script/email-request-attachments/.clasp.json` (needs update)
