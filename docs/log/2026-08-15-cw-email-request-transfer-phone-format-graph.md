# 2026-08-15: CW Email Request — Keycard Number optional, Transfer drops contact fields, phone auto-format, Graph re-enabled

Four changes together: added `"500 W Warner Ave Ste 200, Santa Ana, CA 92707"` to `public/locations.js` (shared by every consumer); Keycard Number became optional (required-mark removed, `functions/emailRequest.js`'s `requiredBase` no longer includes `"keycard"`, entry line only prints it if non-empty); Transfer entries stopped requiring/showing Tenant Email/Phone (`needsContact` in `buildKeycard()` is now `action === "activate"` only, `toggleThisContactRow()`'s `hideContact` now also covers Transfer, with a separate `hideFee` flag keeping Fee visible for Transfer); and every phone input (Keycard's Tenant Phone plus Wi-Fi/Printer/Phone/App/Laptop's Phone) now auto-formats live to `###-###-####` via a shared `eraFormatPhone()` client-side and a new `formatPhoneForDisplay()` server-side backstop, both stripping non-digits first so any pasted shape normalizes the same way. `ERA_SUBMIT_MODE` flipped back to `"graph"` now that Azure AD `Mail.Send` is granted again.

Files: `public/locations.js`, `public/index.html`, `functions/emailRequest.js`
